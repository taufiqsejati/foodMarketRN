import React from 'react';
import {View} from 'react-native';

const Gap = ({width, height}) => {
  return (
    <View
      style={{
        height: height,
        width: width,
      }}
    />
  );
};

export default Gap;
